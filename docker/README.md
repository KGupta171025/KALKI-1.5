# /docker
Dockerfiles and multi-stage compose profiles for api services, background workers, and datastores.
- `docker-compose.yml`
- `backend.Dockerfile`
- `frontend.Dockerfile`
- `sandbox.Dockerfile`
